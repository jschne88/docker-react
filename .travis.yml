language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t jared/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true jared/docker-react npm run test -- --coverage